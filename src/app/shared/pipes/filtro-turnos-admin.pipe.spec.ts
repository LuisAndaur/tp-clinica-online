import { FiltroTurnosAdminPipe } from './filtro-turnos-admin.pipe';

describe('FiltroTurnosAdminPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroTurnosAdminPipe();
    expect(pipe).toBeTruthy();
  });
});
